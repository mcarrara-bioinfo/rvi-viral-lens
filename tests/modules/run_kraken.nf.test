nextflow_process {

    name "Test Process run_kraken"
    script "modules/run_kraken.nf"
    process "run_kraken"

    test("Should run without failures") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                
                """
                // define inputs of the process here. Example:
                input[0] = tuple(params.meta, [file("${projectDir}/tests/test_data/test_1.fastq.gz"),file("${projectDir}/tests/test_data/test_2.fastq.gz")])
                input[1] = file("${projectDir}/tests/test_data/minimal")
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
