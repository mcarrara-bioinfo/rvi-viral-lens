nextflow_process {

    name "Test Process compute_percentage_covered_bases"
    script "modules/compute_percentage_covered_bases.nf"
    process "compute_percentage_covered_bases"

    test("Should run without failures") {

        when {
            params {
                // define parameters here. Example:
                meta = [
                    id:"test.9000000",
                    sample_id:"test",
                    taxid:9000000
                ]
                test_loc="${projectDir}/tests/test_data"
            }
            process {
                """
                // define inputs of the process here. Example:
                input[0] = tuple(
                    params.meta,
                    file("${params.test_loc}/test_consensuses/test.9000000.fa"),
                    file("${params.test_loc}/test_references/9000000.fa")
                )
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
