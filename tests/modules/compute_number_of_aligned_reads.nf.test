nextflow_process {

    name "Test Process compute_number_of_aligned_reads"
    script "modules/compute_number_of_aligned_reads.nf"
    process "compute_number_of_aligned_reads"

    test("Should run without failures") {

        when {
            params {
                meta = [
                    id:"test.9000000",
                    sample_id:"test",
                    taxid:9000000
                ]
                test_loc="${projectDir}/tests/test_data"
            }
            process {
                """
                // define inputs of the process here. Example:
                input[0] = tuple(
                    params.meta,
                    file("${params.test_loc}/test_alignments/test2.9000000.sorted.bam")
                )
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
