nextflow_process {

    name "Test Process filter_taxids"
    script "modules/filter_taxid.nf"
    process "filter_taxids"

    test("Should run without failures") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
                kraken_report="${projectDir}/tests/test_data/test_kraken_databases/test.report.txt"
            }
            process {
                """
                // define inputs of the process here. Example:
                input[0] = tuple(params.meta, file(params.kraken_report))
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
